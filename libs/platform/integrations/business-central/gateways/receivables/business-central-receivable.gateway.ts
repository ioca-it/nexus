export type {
  BusinessCentralReceivable,
  BusinessCentralReceivableGateway,
} from './business-central-receivable.types';

// The standard Business Central API v2.0 does not expose the complete customer
// ledger entry shape required by this contract. A custom API page is required.
