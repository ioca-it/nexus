# Orders workflow

`ORDER_WORKFLOW` is the single source of truth for the six approved Orders
states and their six transitions. It is evaluated by Platform's workflow
engine; this module does not implement a parallel engine or transition use
cases.
