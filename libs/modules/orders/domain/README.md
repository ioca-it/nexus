# Orders domain

The domain defines immutable draft orders, authorized-price line snapshots,
nominal NEXUS identifiers, centralized two-decimal amount rounding, and the
abstract repository contract. It has no dependency on Commercial Catalog,
Dataverse, Business Central, HTTP, or NestJS.

Only `DRAFT` is approved. No transition operation exists in this increment.
