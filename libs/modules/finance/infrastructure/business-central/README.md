# Business Central Finance adapters

These read-only adapters delegate once to the existing Business Central invoice
or credit memo gateway and map its normalized records into frozen Finance domain
models. They contain no authorization, Dataverse access, HTTP implementation or
write behavior.
